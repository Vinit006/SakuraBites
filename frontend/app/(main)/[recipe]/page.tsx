import React from 'react'

/*------------------------------------
Prop = recipe's slug,
--------------------------------------*/

type Props = {}

export default function page({}: Props) {
  return (
    <section>recipe specific</section>
  )
}